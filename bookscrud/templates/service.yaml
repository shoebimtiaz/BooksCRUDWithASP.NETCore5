apiVersion: v1
kind: Service
metadata:
  name: {{ include "bookscrud.fullname" . }}
  labels:
    {{- include "bookscrud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bookscrud.selectorLabels" . | nindent 4 }}
